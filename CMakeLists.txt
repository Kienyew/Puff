# This file is generated by QtCreator
# and modified by human
cmake_minimum_required(VERSION 3.16)

project(Puff VERSION 0.1 LANGUAGES CXX)

set(CMAKE_AUTOMOC ON)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

find_package(Qt6 6.2 COMPONENTS Quick Widgets Multimedia REQUIRED)

qt_add_executable(appPuff
    main.cpp
    lyricsmodel.h
    lyricsmodel.cpp
    lrc.h
    lrc.cpp
)

qt_add_qml_module(appPuff
    URI Puff
    VERSION 1.0
    QML_FILES
    main.qml
    LyricsView.qml
)

set_target_properties(appPuff PROPERTIES
    MACOSX_BUNDLE_GUI_IDENTIFIER my.example.com
    MACOSX_BUNDLE_BUNDLE_VERSION ${PROJECT_VERSION}
    MACOSX_BUNDLE_SHORT_VERSION_STRING ${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}
    MACOSX_BUNDLE TRUE
    WIN32_EXECUTABLE TRUE
)

target_compile_definitions(appPuff
    PRIVATE $<$<OR:$<CONFIG:Debug>,$<CONFIG:RelWithDebInfo>>:QT_QML_DEBUG>)
target_link_libraries(appPuff
    PRIVATE Qt6::Quick Qt6::Widgets Qt6::Multimedia)
